#OOP AGENDA

This is a very basic README.
To run the program, open the project directory in a terminal, and execute the compilied file (agenda_program) like this: ./agenda_program
If you make changes, open the terminal again in the same directory, write (make) to compile the project again, and then execute it with: ./agenda_program
